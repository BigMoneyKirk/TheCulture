import { TopMarginDirective } from './top-margin.directive';

describe('TopMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new TopMarginDirective();
    expect(directive).toBeTruthy();
  });
});
